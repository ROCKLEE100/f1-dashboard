
apiVersion: v1
kind: Service
metadata:
  name: f1-backend-metrics
  namespace: default
  labels:
    app: f1-backend
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 8000
      targetPort: 8000
  selector:
    app: f1-backend

---

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: f1-backend-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: f1-backend
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
